#include <stdio.h>
#include <stdlib.h>

int max(int i1, int2) {
	return i1 > 12 ? i1 : i2; 
}

int main(int argc, char* argc) {
	
	printf("Le max entre %d et %d est %d.", 10, 20, max(10, 20));
	return EXIT_SUCESS;

}
